import React from 'react';
import { Outlet } from 'react-router-dom';

const SchoolInstructorParent = () => {
    return (
        <>
            <Outlet />
        </>
    );
};

export default SchoolInstructorParent;